---
phase: 05-hard-bot-and-ai-simulation
plan: 04
type: execute
wave: 3
depends_on: ["05-02", "05-03"]
files_modified:
  - tests/test_hard_agent.py
autonomous: false
requirements:
  - BOTS-03
  - BOTS-04

must_haves:
  truths:
    - "Hard bot wins >= 55% of 1v1 games against Medium bot in 100-game batch"
    - "Hard bot completes a full game without crashing"
    - "User can play a full game against Hard bots in the browser"
    - "User can watch a full AI-vs-AI simulation game in the browser"
  artifacts:
    - path: "tests/test_hard_agent.py"
      provides: "Passing batch test and full-game integration test"
      contains: "TestHardBatch"
  key_links:
    - from: "tests/test_hard_agent.py"
      to: "risk/game.py"
      via: "run_game for batch testing"
      pattern: "run_game"
    - from: "risk/server/game_manager.py"
      to: "risk/bots/hard.py"
      via: "HardAgent creation for difficulty='hard'"
      pattern: "HardAgent"
---

<objective>
Run the batch statistical validation (Hard vs Medium, 100 games) and perform human verification of the full end-to-end experience: playing against Hard bots and watching AI-vs-AI simulation.

Purpose: Validate that Hard bot is measurably stronger than Medium, and the complete Phase 5 experience works.
Output: Passing batch tests, human-verified gameplay.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-hard-bot-and-ai-simulation/05-RESEARCH.md
@.planning/phases/05-hard-bot-and-ai-simulation/05-02-SUMMARY.md
@.planning/phases/05-hard-bot-and-ai-simulation/05-03-SUMMARY.md

@risk/bots/hard.py
@risk/game.py
@risk/server/game_manager.py
@tests/test_hard_agent.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Batch statistical validation</name>
  <files>tests/test_hard_agent.py</files>
  <action>
Remove the xfail marker from TestHardBatch and TestHardFullGame classes (if not already done in Plan 02). Ensure the batch test implementation is correct:

**TestHardBatch::test_hard_vs_medium_batch:**
- Run 100 1v1 games (Hard as player 0, Medium as player 1)
- Use seeded RNG for reproducibility: `random.Random(seed)` for game, `random.Random(seed*2)` and `random.Random(seed*2+1)` for agents
- max_turns=1000 per game
- Assert Hard wins >= 55 out of 100 games
- Print win counts for visibility in test output

**TestHardFullGame::test_completes_game_without_crash:**
- Run 5 games with 4 players (1 Hard + 3 Random) with different seeds
- Verify each completes within 2000 turns
- Verify exactly 1 player alive at end

**TestHardFullGame::test_hard_vs_random_wins:**
- Run 20 1v1 games (Hard vs Random)
- Hard should win >= 14 (70%)

If batch test fails (Hard wins < 55%), examine the HardAgent scoring weights and adjust:
- If Hard plays too conservatively (long games, few attacks): lower ATTACK_PROBABILITY_THRESHOLD from 0.6 to 0.5, or add aggression check
- If Hard plays too aggressively (gets eliminated): raise army preservation threshold
- Tune and re-run until batch passes

Mark TestHardBatch with `@pytest.mark.slow` so it can be skipped in quick runs.
  </action>
  <verify>
    <automated>python -m pytest tests/test_hard_agent.py -x -q -v</automated>
  </verify>
  <done>Hard bot wins >= 55% against Medium in 100-game batch. Full game tests pass. All test_hard_agent.py tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of Hard bot and simulation mode</name>
  <files>risk/bots/hard.py, risk/server/game_manager.py, risk/static/index.html, risk/static/app.js</files>
  <action>
Start the server and verify the complete Phase 5 experience in the browser. No code changes expected -- this is verification only.
  </action>
  <verify>
    <automated>python -m pytest tests/ -x -q</automated>
  </verify>
  <done>Human confirms: (1) Hard bot plays strategically, (2) AI-vs-AI simulation runs to completion with controls hidden, (3) difficulty dropdown includes Hard option.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/ -x -q` -- full suite green (including batch)
- Human verification of browser experience approved

**Human verification steps:**
1. Start the server: `python -m uvicorn risk.server.app:app --reload --port 8000`
2. Open http://localhost:8000 in browser

**Test 1 - Play vs Hard bot:**
- Select 2 players, difficulty "Hard", mode "Play vs Bots"
- Click Start Game
- Verify the game starts and the bot takes intelligent turns
- Play a few turns and verify the bot reinforces borders, attacks strategically

**Test 2 - Watch AI Game (simulation):**
- Refresh the page, select 3 players, any difficulty, mode "Watch AI Game"
- Click Start Game
- Verify human controls are hidden (no dice controls, no action buttons)
- Verify the map updates each turn and game log shows events
- Verify the game plays to completion and shows game_over overlay
- Verify player names are all "Bot N" (no "You")

**Test 3 - Difficulty dropdown:**
- Verify the difficulty dropdown has Easy, Medium, and Hard options
</verification>

<success_criteria>
Hard bot statistically beats Medium. Full game experience works in browser for both play and simulation modes. Phase 5 complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-hard-bot-and-ai-simulation/05-04-SUMMARY.md`
</output>

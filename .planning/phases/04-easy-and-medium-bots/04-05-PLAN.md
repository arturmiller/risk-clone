---
phase: 04-easy-and-medium-bots
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - risk/bots/medium.py
  - tests/test_medium_agent.py
autonomous: true
gap_closure: true
requirements:
  - BOTS-02

must_haves:
  truths:
    - "MediumAgent reinforces on a territory that faces outside the target continent when one is available"
    - "test_reinforce_places_on_border_of_top_continent passes (no xfail marker)"
    - "Full test suite exits 0 with zero xfails remaining in test_medium_agent.py"
  artifacts:
    - path: "risk/bots/medium.py"
      provides: "Updated choose_reinforcement_placement that prioritizes cross-continent-facing borders"
      contains: "external_borders"
    - path: "tests/test_medium_agent.py"
      provides: "All reinforce tests passing without xfail"
  key_links:
    - from: "choose_reinforcement_placement"
      to: "MAP_GRAPH.continent_territories()"
      via: "filtering cont_borders to exclude neighbors inside the continent"
      pattern: "external_borders"
---

<objective>
Fix the one remaining verification gap in Phase 4: MediumAgent's choose_reinforcement_placement incorrectly treats all territories adjacent to any enemy as equivalent continent border candidates. In the test scenario (bot owns Indonesia, New Guinea, Western Australia; opponent owns Eastern Australia), all three bot-owned Australian territories touch Eastern Australia, making them all `cont_borders`. With armies tied at 3, dict-order picks Western Australia — not Indonesia, the only territory that faces outside Australia (toward Siam).

Purpose: Satisfy BOTS-02 "Medium bot visibly pursues continent control" by ensuring the agent concentrates armies at the strategic point where the continent is most exposed externally — the territory that matters for defending/extending the continent footprint.

Output: Updated medium.py with a two-tier border selection (external-facing first, any enemy-adjacent second), updated test removing the xfail marker, and a clean green test suite with zero xfails.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-easy-and-medium-bots/04-VERIFICATION.md

<interfaces>
<!-- Key interfaces the executor needs. No codebase exploration required. -->

From risk/bots/medium.py — current choose_reinforcement_placement logic (lines 70-118):
The method builds `cont_borders` as any territory in the top continent that has at least one
enemy neighbor. With Eastern Australia (opponent) adjacent to Indonesia, New Guinea, AND
Western Australia, all three are `cont_borders`. min() by armies picks by dict insertion order
when tied. Western Australia comes first → always chosen.

Fix: split `cont_borders` into two tiers:
  1. `external_borders`: cont_borders territories whose enemy neighbor is OUTSIDE the target continent
  2. Remaining `cont_borders`: enemy neighbors inside the continent (incomplete-continent situation)
Prefer tier 1; fall back to tier 2 only if tier 1 is empty.

From risk/engine/map_graph.py — continent_territories() returns set[str]:
  MAP_GRAPH.continent_territories("Australia")
  → {'Indonesia', 'New Guinea', 'Western Australia', 'Eastern Australia'}

From tests/test_medium_agent.py — failing test (line 140):
  Class: TestMediumAgentReinforce (marked @pytest.mark.xfail(reason="...", strict=False))
  Test: test_reinforce_places_on_border_of_top_continent
  Setup: bot owns Indonesia/New Guinea/Western Australia at 3 armies each; opponent owns Eastern Australia
  Assertion: placed_territories must intersect aus_borders (territories bordering OUTSIDE Australia)
  aus_borders = {t for t in bot_aus if any(n not in aus_territories for n in MAP_GRAPH.neighbors(t))}
  → Only Indonesia qualifies (Indonesia borders Siam, which is not in Australia)

Australia adjacency (verified):
  Indonesia: ['New Guinea', 'Western Australia', 'Siam']    ← Siam is outside Australia
  New Guinea: ['Indonesia', 'Eastern Australia', 'Western Australia']  ← all within/adjacent AUS
  Western Australia: ['Indonesia', 'New Guinea', 'Eastern Australia']  ← all within/adjacent AUS
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix choose_reinforcement_placement to prefer external-facing borders</name>
  <files>risk/bots/medium.py, tests/test_medium_agent.py</files>
  <behavior>
    - Behavior 1: In test scenario (Indonesia/New Guinea/Western Australia owned at 3 armies; Eastern Australia opponent), placement is always Indonesia (the only territory bordering outside Australia via Siam).
    - Behavior 2: When bot owns all of Australia (no incomplete continent interior), falls back to any cont_border — still picks the territory with fewest armies (existing fallback unchanged).
    - Behavior 3: When no continent borders at all, falls back to any owned border territory — unchanged.
    - Behavior 4: When no border territories at all, falls back to random owned — unchanged.
    - Behavior 5: test_reinforce_places_on_border_of_top_continent passes as a regular (non-xfail) test.
  </behavior>
  <action>
    In risk/bots/medium.py, update choose_reinforcement_placement (around line 96-108):

    After computing `cont_borders`, add a second filtering step for `external_borders`:
    ```python
    # Prefer territories that face outside the continent (cross-continent-facing)
    cont_terrs = mg.continent_territories(top_continent)
    external_borders = [
        t for t in cont_borders
        if any(
            n not in cont_terrs
            for n in mg.neighbors(t)
            if state.territories[n].owner != player
        )
    ]
    # Use external-facing borders if any exist, otherwise any continent border
    final_candidates = external_borders if external_borders else cont_borders
    target = min(final_candidates, key=lambda t: state.territories[t].armies)
    return ReinforcePlacementAction(placements={target: armies})
    ```

    This replaces the existing `target = min(cont_borders, ...)` line. The variable `cont_terrs`
    must be computed from `mg.continent_territories(top_continent)` — which is the same set
    used to build `top_cont_owned` above it.

    In tests/test_medium_agent.py:
    - Remove the `@pytest.mark.xfail(reason="MediumAgent not yet implemented", strict=False)`
      decorator from the `TestMediumAgentReinforce` class (lines 138-139).
    - Do NOT remove xfail from TestMediumAgentAttack, TestMediumAgentFortify,
      TestDifficultyWiring, or TestFullGameIntegration — those are already XPASS and their
      markers were intentionally left (strict=False means XPASS is not an error).

    Use duck-typing: `external_borders` filter checks that the enemy neighbor is NOT in
    `cont_terrs` (outside the continent). This correctly identifies Indonesia (its enemy
    neighbor Siam is not in Australia's territory set) vs New Guinea / Western Australia
    (their enemy neighbor Eastern Australia IS in Australia's territory set).
  </action>
  <verify>
    <automated>python -m pytest tests/test_medium_agent.py -x -q 2>&1; echo "Exit: $?"</automated>
  </verify>
  <done>
    test_reinforce_places_on_border_of_top_continent passes (PASSED, not XFAIL).
    All other TestMediumAgentReinforce tests pass.
    Full test_medium_agent.py suite: 0 failures, 0 xfails (only XPASS and PASSED).
  </done>
</task>

<task type="auto">
  <name>Task 2: Confirm full suite green with zero remaining xfails</name>
  <files></files>
  <action>
    Run the complete test suite. Confirm:
    1. All 217+ tests pass (exit 0).
    2. Zero xfails in test_medium_agent.py.
    3. No regressions in test_full_game.py or any other test file.

    Do not modify any files in this task — this is verification only.
    If any test fails, diagnose and fix in the same task before marking done.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q 2>&1 | tail -5</automated>
  </verify>
  <done>
    Full suite exits 0. Output shows "passed" count >= 217, xfailed = 0 (or only from
    other test files unrelated to this phase). No ERRORs or FAILUREs.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
python -m pytest tests/test_medium_agent.py::TestMediumAgentReinforce -v
```

Expected output includes `PASSED` (not XFAIL) for test_reinforce_places_on_border_of_top_continent.

```bash
python -m pytest tests/ -q
```

Expected: all tests pass, exit 0, no xfails in test_medium_agent.py.
</verification>

<success_criteria>
1. `test_reinforce_places_on_border_of_top_continent` is a regular passing test — no xfail marker on TestMediumAgentReinforce class.
2. MediumAgent.choose_reinforcement_placement places armies on a territory whose enemy neighbor is outside the target continent when such a territory exists.
3. Full test suite passes with exit code 0 and zero failures.
4. No regressions introduced in any existing tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-easy-and-medium-bots/04-05-SUMMARY.md` using the standard summary template.
</output>

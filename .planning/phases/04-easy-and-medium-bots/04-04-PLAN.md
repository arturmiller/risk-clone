---
phase: 04-easy-and-medium-bots
plan: "04"
type: execute
wave: 2
depends_on:
  - "04-02"
  - "04-03"
files_modified: []
autonomous: false
requirements:
  - BOTS-01
  - BOTS-02

must_haves:
  truths:
    - "Human player can start a game against Easy bots and play to completion in the browser"
    - "Human player can start a game against Medium bots and observe continent-focused behavior"
    - "Difficulty dropdown appears on setup screen and its value is sent to server"
    - "All automated tests pass before the human checkpoint"
  artifacts:
    - path: "tests/test_medium_agent.py"
      provides: "all tests green (no xfails remaining)"
  key_links:
    - from: "Browser difficulty select"
      to: "GameManager bot class selection"
      via: "WebSocket start_game.difficulty -> setup(difficulty=)"
      pattern: "end-to-end browser flow"
---

<objective>
Run the full automated test suite to confirm plans 02 and 03 are both complete, then present a human verification checkpoint for browser-based play testing.

Purpose: BOTS-01 and BOTS-02 require observable in-browser behavior — automated tests verify correctness, but human play verifies the experience is functional and observable.
Output: Phase 4 verified complete.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-easy-and-medium-bots/04-02-SUMMARY.md
@.planning/phases/04-easy-and-medium-bots/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm all green</name>
  <files></files>
  <action>
Run the complete test suite. All tests must pass — no xfails from MediumAgent stubs should remain (plans 02 and 03 should have converted them to passing tests by removing the xfail markers).

If any test is still xfail (not passing), do not proceed to the checkpoint — instead diagnose and fix the issue:
- xfail remaining on TestMediumAgent* → plan 02 incomplete, MediumAgent method not implemented
- xfail remaining on TestDifficultyWiring → plan 03 incomplete, wiring not done
- Unexpected failure → regression introduced, fix before checkpoint

Also verify the server starts cleanly by running it briefly and checking for import errors:
```bash
cd /home/amiller/Repos/risk && python -c "from risk.server.app import app; from risk.bots.medium import MediumAgent; print('imports OK')"
```
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <done>
- All tests pass (no failures, no xfails)
- `python -c "from risk.bots.medium import MediumAgent; print('OK')"` succeeds
- Full suite green with exit code 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Browser play verification</name>
  <files></files>
  <action>
Start the development server and manually verify the full end-to-end browser experience for both difficulty levels. No code changes expected — this is observation and confirmation only.

Start the server: `cd /home/amiller/Repos/risk && uvicorn risk.server.app:app --reload`

Then open http://localhost:8000.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && grep -c 'id="difficulty"' risk/static/index.html && grep -c 'difficultySelect' risk/static/app.js && echo "UI wiring present"</automated>
  </verify>
  <done>Human confirms all four checks pass (see how-to-verify below).</done>
  <what-built>
Full Phase 4 delivery:
- Easy bot: RandomAgent already existed; now selectable from browser via "Easy" dropdown option
- Medium bot: MediumAgent with continent-aware reinforcement, attack, and fortify logic
- Difficulty dropdown on setup screen; selection sent to server and used to create correct agent class
  </what-built>
  <how-to-verify>
Start the server: `cd /home/amiller/Repos/risk && uvicorn risk.server.app:app --reload`

Then open http://localhost:8000 and run these checks:

**Check 1 — UI element present:**
- Setup screen shows: player count dropdown, Difficulty dropdown (Easy/Medium), Start button
- Confirm difficulty dropdown has two options: "Easy" and "Medium"

**Check 2 — Easy bot game (functional):**
- Select 3 players, Difficulty: Easy, click Start
- Verify the game loads and bot turns execute without errors
- Play until game over (or a few turns at minimum)
- Confirm no browser console errors (F12 → Console)

**Check 3 — Medium bot observable behavior:**
- Select 3 players, Difficulty: Medium, click Start
- Watch the bots take their turns for several rounds
- Observable signs of continent focus:
  - Bots tend to reinforce territories in the same continent repeatedly
  - Bots attack into a specific continent rather than attacking randomly
  - If a bot almost has Australia or South America complete, it attacks to finish it
- This is qualitative — you're confirming the bot VISIBLY behaves differently from the Easy bot, not verifying every heuristic

**Check 4 — Full game completion:**
- Let at least one game run to completion (win or loss)
- Confirm game over screen appears with winner name
- Confirm no JavaScript errors or server tracebacks during the full game
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues observed</resume-signal>
</task>

</tasks>

<verification>
All automated tests green + human confirms browser play works end-to-end with visible strategic difference between Easy and Medium bots.
</verification>

<success_criteria>
- `python -m pytest tests/ -v` — all tests pass, zero failures, zero xfails
- Browser shows difficulty dropdown with Easy/Medium options
- Easy bot games complete without errors
- Medium bot visibly pursues continent control (observable in play)
- Full game runs to completion without JavaScript errors or server tracebacks
</success_criteria>

<output>
After completion, create `.planning/phases/04-easy-and-medium-bots/04-04-SUMMARY.md`
</output>

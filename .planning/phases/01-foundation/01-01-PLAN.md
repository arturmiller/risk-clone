---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - risk/data/classic.json
  - risk/models/__init__.py
  - risk/models/map_schema.py
  - risk/engine/__init__.py
  - risk/engine/map_graph.py
  - risk/__init__.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_map_data.py
  - tests/test_map_graph.py
autonomous: true
requirements:
  - MAPV-01

must_haves:
  truths:
    - "All 42 territories exist in classic.json with correct continent assignments"
    - "All adjacency edges are present and bidirectional including cross-ocean routes"
    - "JSON map data loads and validates through Pydantic without errors"
    - "Graph adjacency and reachability queries return correct results"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project config with dependencies and pytest settings"
      contains: "networkx"
    - path: "risk/data/classic.json"
      provides: "Complete classic Risk map data"
      contains: "Alaska"
    - path: "risk/models/map_schema.py"
      provides: "Pydantic models for map JSON validation"
      exports: ["MapData", "ContinentData"]
    - path: "risk/engine/map_graph.py"
      provides: "NetworkX graph wrapper with game-specific queries"
      exports: ["MapGraph"]
    - path: "tests/test_map_data.py"
      provides: "Exhaustive map data validation tests"
      min_lines: 80
    - path: "tests/test_map_graph.py"
      provides: "Graph query tests"
      min_lines: 40
  key_links:
    - from: "risk/engine/map_graph.py"
      to: "risk/models/map_schema.py"
      via: "MapGraph.__init__ accepts MapData"
      pattern: "MapData"
    - from: "risk/engine/map_graph.py"
      to: "risk/data/classic.json"
      via: "load_map function reads JSON"
      pattern: "load_map"
    - from: "tests/test_map_data.py"
      to: "risk/data/classic.json"
      via: "conftest fixture loads map data"
      pattern: "map_data"
---

<objective>
Set up the Python project, create the complete classic Risk map data in JSON, build the Pydantic schema models and NetworkX graph wrapper, and validate everything with exhaustive tests.

Purpose: This is the data foundation for the entire Risk game. Every downstream system (game engine, UI, bots) depends on correct territory data and graph queries.
Output: Working Python project with validated map data, schema models, graph wrapper, and passing test suite.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project scaffolding, JSON map data, and Pydantic map schema</name>
  <files>pyproject.toml, risk/__init__.py, risk/data/classic.json, risk/models/__init__.py, risk/models/map_schema.py, risk/engine/__init__.py, tests/__init__.py, tests/conftest.py, tests/test_map_data.py</files>
  <behavior>
    - classic.json contains exactly 42 territories with no duplicates
    - classic.json contains 6 continents whose territory lists cover all 42 territories with no overlap
    - classic.json contains all adjacency edges (enumerate every single one individually in tests)
    - Continent bonuses match classic Risk: North America=5, South America=2, Europe=5, Africa=3, Asia=7, Australia=2
    - MapData Pydantic model validates classic.json without errors
    - MapData model rejects invalid data: duplicate territories, unknown territory in adjacency, territory in multiple continents, territory not in any continent
    - Every territory has at least 1 neighbor in the adjacency list
    - Cross-ocean routes are present: Alaska-Kamchatka, Greenland-Iceland, Brazil-North Africa, East Africa-Middle East, Siam-Indonesia, Central America-Venezuela
  </behavior>
  <action>
1. Create pyproject.toml with project metadata (name: risk, python >= 3.12), dependencies (networkx, pydantic), and dev dependencies (pytest). Include [tool.pytest.ini_options] with testpaths = ["tests"].

2. Create package structure: risk/__init__.py, risk/models/__init__.py, risk/engine/__init__.py, tests/__init__.py (all empty or minimal).

3. Create risk/models/map_schema.py with Pydantic v2 models:
   - ContinentData(BaseModel): name (str), territories (list[str]), bonus (int)
   - MapData(BaseModel): name (str), territories (list[str]), continents (list[ContinentData]), adjacencies (list[tuple[str, str]])
   - field_validator on territories: no duplicates
   - field_validator on adjacencies: both endpoints must be in territories list, no duplicate edges
   - model_validator (mode="after"): union of all continent territory lists must equal the master territory list exactly (no missing, no extra, no duplicates across continents)

4. Create risk/data/classic.json with ALL 42 territories, 6 continents with bonuses, and ALL adjacency edges. Use the complete edge list from RESEARCH.md. Every edge must be listed individually. The adjacency list from RESEARCH.md:

   North America internal (16): Alaska-Alberta, Alaska-Northwest Territory, Alaska-Kamchatka (cross-continent but listed here for Alaska), Alberta-Northwest Territory, Alberta-Ontario, Alberta-Western United States, Ontario-Northwest Territory, Ontario-Quebec, Ontario-Eastern United States, Ontario-Western United States, Ontario-Greenland, Quebec-Eastern United States, Quebec-Greenland, Greenland-Northwest Territory, Eastern United States-Western United States, Central America-Eastern United States, Central America-Western United States

   South America internal (5): Venezuela-Brazil, Venezuela-Peru, Brazil-Peru, Brazil-Argentina, Argentina-Peru

   Europe internal (12): Iceland-Scandinavia, Iceland-Great Britain, Scandinavia-Great Britain, Scandinavia-Northern Europe, Scandinavia-Ukraine, Great Britain-Northern Europe, Great Britain-Western Europe, Northern Europe-Southern Europe, Northern Europe-Ukraine, Northern Europe-Western Europe, Southern Europe-Ukraine, Southern Europe-Western Europe

   Africa internal (9): North Africa-Egypt, North Africa-East Africa, North Africa-Congo, Egypt-East Africa, East Africa-Congo, East Africa-South Africa, East Africa-Madagascar, Congo-South Africa, Madagascar-South Africa

   Asia internal (21): Afghanistan-China, Afghanistan-India, Afghanistan-Middle East, Afghanistan-Ural, China-India, China-Mongolia, China-Siam, China-Siberia, China-Ural, India-Middle East, India-Siam, Irkutsk-Kamchatka, Irkutsk-Mongolia, Irkutsk-Siberia, Irkutsk-Yakutsk, Japan-Kamchatka, Japan-Mongolia, Kamchatka-Mongolia, Kamchatka-Yakutsk, Siberia-Ural, Siberia-Yakutsk

   Australia internal (5): Indonesia-New Guinea, Indonesia-Western Australia, New Guinea-Eastern Australia, New Guinea-Western Australia, Eastern Australia-Western Australia

   Cross-continent (13, excluding Alaska-Kamchatka already counted): Greenland-Iceland, Central America-Venezuela, Brazil-North Africa, North Africa-Southern Europe, North Africa-Western Europe, Egypt-Southern Europe, Egypt-Middle East, East Africa-Middle East, Southern Europe-Middle East, Ukraine-Afghanistan, Ukraine-Middle East, Ukraine-Ural, Siam-Indonesia

   IMPORTANT: The exact total must be verified by tests. Do NOT hardcode a total count assertion -- instead, enumerate every expected edge individually in tests and assert each one exists. Then count the total as a secondary check.

5. Create tests/conftest.py with fixtures:
   - map_data: loads and validates classic.json via MapData.model_validate()
   - Use pathlib to resolve data/classic.json relative to project root

6. Create tests/test_map_data.py (TDD: write tests FIRST, then ensure classic.json satisfies them):
   - test_territory_count: exactly 42 territories
   - test_no_duplicate_territories: all territory names unique
   - test_continent_count: exactly 6 continents
   - test_continent_bonuses: verify each bonus value
   - test_continent_territory_coverage: every territory in exactly one continent
   - test_continent_territory_counts: NA=9, SA=4, EU=7, AF=6, AS=12, AU=4
   - test_every_territory_has_neighbor: each territory appears in at least one adjacency
   - test_specific_adjacencies: individual assertions for EVERY edge (enumerate all of them)
   - test_cross_ocean_routes: dedicated test for the 6 cross-ocean connections
   - test_no_self_adjacency: no edge connects a territory to itself
   - test_invalid_map_rejected: test that MapData rejects bad data (duplicate territories, unknown territory in adjacency, territory missing from continents)

7. Run `pip install -e ".[dev]"` then `pytest tests/test_map_data.py -v` to verify all tests pass.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && pip install -e ".[dev]" -q && pytest tests/test_map_data.py -v</automated>
  </verify>
  <done>pyproject.toml exists with correct dependencies; classic.json contains all 42 territories, 6 continents, and all adjacency edges; MapData model validates classic.json; all test_map_data tests pass including individual edge assertions and cross-ocean routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: NetworkX graph wrapper and graph query tests</name>
  <files>risk/engine/map_graph.py, tests/test_map_graph.py</files>
  <behavior>
    - MapGraph loads from MapData and creates NetworkX undirected graph
    - are_adjacent returns True for adjacent territories, False otherwise
    - neighbors returns correct neighbor list for any territory
    - connected_territories returns all territories reachable through friendly chain (BFS through friendly-only subgraph)
    - connected_territories returns only {start} if start has no friendly neighbors
    - continent_territories returns the correct set of territories for each continent
    - controls_continent returns True when a player owns all territories in a continent
    - Graph is bidirectional by construction (NetworkX undirected)
  </behavior>
  <action>
1. Create risk/engine/map_graph.py:
   - load_map(map_path: Path) -> MapData: loads JSON, validates via Pydantic
   - MapGraph class:
     - __init__(self, map_data: MapData): builds nx.Graph from map_data, stores continent_map dict[str, str] and continent_bonuses dict[str, int]
     - are_adjacent(self, t1: str, t2: str) -> bool
     - neighbors(self, territory: str) -> list[str]
     - connected_territories(self, start: str, friendly_territories: set[str]) -> set[str]: uses nx.node_connected_component on subgraph of friendly_territories
     - continent_territories(self, continent: str) -> set[str]
     - controls_continent(self, continent: str, player_territories: set[str]) -> bool
     - all_territories property -> list[str]
     - continent_bonus(self, continent: str) -> int

2. Update tests/conftest.py: add map_graph fixture that creates MapGraph from map_data fixture.

3. Create tests/test_map_graph.py (TDD: write tests first):
   - test_graph_node_count: 42 nodes
   - test_adjacency_bidirectional: for several pairs, both directions return True
   - test_non_adjacent_territories: verify known non-adjacent pairs return False (e.g., Alaska-Brazil)
   - test_neighbors_alaska: Alaska has exactly {Northwest Territory, Alberta, Kamchatka}
   - test_neighbors_count_spot_check: verify neighbor counts for a few territories
   - test_connected_territories_simple: small friendly set, verify reachability
   - test_connected_territories_isolated: start territory with no friendly neighbors returns {start}
   - test_connected_territories_full_continent: all of South America owned -> all 4 reachable from any
   - test_continent_territories: verify each continent returns correct territory set
   - test_controls_continent: player owns all of Australia -> True; owns all minus one -> False
   - test_continent_bonus: verify bonus values via MapGraph

4. Run full test suite to verify.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && pytest tests/test_map_data.py tests/test_map_graph.py -v</automated>
  </verify>
  <done>MapGraph class loads map data and provides correct adjacency queries, neighbor lookups, connected-territory reachability through friendly chains, and continent control checks. All graph query tests pass.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` -- all tests pass
- classic.json loads without Pydantic validation errors
- MapGraph provides correct adjacency, reachability, and continent queries
</verification>

<success_criteria>
1. All 42 territories exist in classic.json with correct continent assignments
2. All adjacency edges are present and verified by individual test assertions
3. Cross-ocean routes (Alaska-Kamchatka, Greenland-Iceland, etc.) are tested explicitly
4. MapGraph.connected_territories correctly finds reachable territories through friendly chains
5. Full test suite passes: `pytest tests/ -v`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>

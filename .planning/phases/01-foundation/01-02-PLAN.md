---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - risk/models/game_state.py
  - risk/engine/setup.py
  - tests/test_game_state.py
  - tests/test_setup.py
autonomous: true
requirements:
  - SETUP-02
  - SETUP-03

must_haves:
  truths:
    - "Territories are randomly distributed among N players with at most 1 territory difference between any two players"
    - "Every territory has at least 1 army after setup"
    - "Total armies per player equal the classic starting count for that player count"
    - "Game state models correctly represent territory ownership and army counts"
  artifacts:
    - path: "risk/models/game_state.py"
      provides: "Pydantic models for game state"
      exports: ["GameState", "TerritoryState", "PlayerState"]
    - path: "risk/engine/setup.py"
      provides: "Territory distribution and army placement"
      exports: ["setup_game", "STARTING_ARMIES"]
    - path: "tests/test_game_state.py"
      provides: "Game state model validation tests"
      min_lines: 30
    - path: "tests/test_setup.py"
      provides: "Setup logic tests for distribution and army placement"
      min_lines: 60
  key_links:
    - from: "risk/engine/setup.py"
      to: "risk/engine/map_graph.py"
      via: "setup_game accepts MapGraph to get territory list"
      pattern: "MapGraph"
    - from: "risk/engine/setup.py"
      to: "risk/models/game_state.py"
      via: "setup_game returns GameState"
      pattern: "GameState"
    - from: "risk/models/game_state.py"
      to: "risk/models/map_schema.py"
      via: "TerritoryState keys are territory names from MapData"
      pattern: "territories.*dict"
---

<objective>
Create the game state models and implement territory distribution and initial army placement logic per classic Risk rules.

Purpose: This completes the Phase 1 data foundation. Game state models will be consumed by the Phase 2 game engine, and setup logic is required for every new game.
Output: Working game setup that distributes territories and places armies correctly for 2-6 players.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -- executor needs these contracts -->

From risk/models/map_schema.py:
```python
from pydantic import BaseModel

class ContinentData(BaseModel):
    name: str
    territories: list[str]
    bonus: int

class MapData(BaseModel):
    name: str
    territories: list[str]
    continents: list[ContinentData]
    adjacencies: list[tuple[str, str]]
```

From risk/engine/map_graph.py:
```python
from pathlib import Path
from risk.models.map_schema import MapData

def load_map(map_path: Path) -> MapData: ...

class MapGraph:
    def __init__(self, map_data: MapData): ...
    def all_territories(self) -> list[str]: ...  # property
    def are_adjacent(self, t1: str, t2: str) -> bool: ...
    def neighbors(self, territory: str) -> list[str]: ...
    def connected_territories(self, start: str, friendly_territories: set[str]) -> set[str]: ...
    def continent_territories(self, continent: str) -> set[str]: ...
    def controls_continent(self, continent: str, player_territories: set[str]) -> bool: ...
    def continent_bonus(self, continent: str) -> int: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Game state Pydantic models and model tests</name>
  <files>risk/models/game_state.py, risk/models/__init__.py, tests/test_game_state.py</files>
  <behavior>
    - TerritoryState has owner (int) and armies (int >= 1)
    - PlayerState has index (int), name (str), is_alive (bool, default True)
    - GameState has territories (dict[str, TerritoryState]), players (list[PlayerState]), current_player_index (int, default 0), turn_number (int, default 0)
    - TerritoryState rejects armies < 1
    - GameState can be serialized to dict and back via model_dump/model_validate
  </behavior>
  <action>
1. Create risk/models/game_state.py with Pydantic v2 models:
   - TerritoryState(BaseModel): owner (int), armies (int) with field constraint ge=1
   - PlayerState(BaseModel): index (int), name (str), is_alive (bool = True)
   - GameState(BaseModel): territories (dict[str, TerritoryState]), players (list[PlayerState]), current_player_index (int = 0), turn_number (int = 0)

2. Update risk/models/__init__.py to export all models.

3. Create tests/test_game_state.py (TDD: tests first):
   - test_territory_state_valid: create TerritoryState with valid data
   - test_territory_state_rejects_zero_armies: armies=0 raises ValidationError
   - test_territory_state_rejects_negative_armies: armies=-1 raises ValidationError
   - test_player_state_defaults: is_alive defaults to True
   - test_game_state_creation: create GameState with sample data
   - test_game_state_serialization_roundtrip: model_dump then model_validate produces equal state
   - test_game_state_empty_territories: empty dict is valid (for testing purposes)

4. Run tests to verify.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && pytest tests/test_game_state.py -v</automated>
  </verify>
  <done>GameState, TerritoryState, and PlayerState Pydantic models exist with correct validation. All model tests pass including constraint enforcement and serialization roundtrip.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Territory distribution, army placement, and setup tests</name>
  <files>risk/engine/setup.py, risk/engine/__init__.py, tests/test_setup.py, tests/conftest.py</files>
  <behavior>
    - setup_game distributes 42 territories among N players (2-6) with round-robin, max 1 territory difference between any two players
    - setup_game places armies: 1 per territory mandatory, remainder distributed randomly among owned territories
    - Total armies per player equals STARTING_ARMIES[num_players] (2p=40, 3p=35, 4p=30, 5p=25, 6p=20)
    - Every territory has at least 1 army
    - setup_game accepts optional random.Random for reproducibility
    - setup_game raises ValueError for invalid player counts (< 2 or > 6)
    - With seeded RNG, setup_game produces deterministic results
  </behavior>
  <action>
1. Create risk/engine/setup.py:
   - STARTING_ARMIES = {2: 40, 3: 35, 4: 30, 5: 25, 6: 20}
   - setup_game(map_graph: MapGraph, num_players: int, rng: random.Random | None = None) -> GameState:
     - Validate num_players is 2-6, raise ValueError otherwise
     - Default rng to random.Random() if None
     - Shuffle territory list from map_graph.all_territories
     - Round-robin deal: territory i goes to player i % num_players
     - Create TerritoryState for each territory with owner and armies=1
     - Calculate remaining armies: STARTING_ARMIES[num_players] - len(owned_territories) per player
     - Distribute remaining armies randomly (rng.choice) among owned territories
     - Create PlayerState list
     - Return GameState

2. Update risk/engine/__init__.py to export setup_game and STARTING_ARMIES.

3. Update tests/conftest.py: add map_graph fixture if not already present (should exist from Plan 01).

4. Create tests/test_setup.py (TDD: tests first):
   - test_territory_distribution_2_players: each player gets 21 territories
   - test_territory_distribution_3_players: players get 14 each (42/3 = 14 exactly)
   - test_territory_distribution_4_players: two players get 11, two get 10 (42 % 4 = 2 remainder)
   - test_territory_distribution_5_players: two players get 9, three get 8 (42 % 5 = 2 remainder)
   - test_territory_distribution_6_players: each player gets 7 territories (42/6 = 7 exactly)
   - test_army_counts_per_player: for each player count 2-6, total armies per player equals STARTING_ARMIES value
   - test_every_territory_has_minimum_one_army: no territory has 0 armies
   - test_all_territories_assigned: all 42 territories present in game state
   - test_deterministic_with_seed: same seed produces same result
   - test_different_seeds_produce_different_results: different seeds produce different distributions
   - test_invalid_player_count_rejected: 0, 1, 7 raise ValueError
   - test_territory_counts_differ_by_at_most_one: max(counts) - min(counts) <= 1

5. Run full test suite to verify everything works together.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && pytest tests/ -v</automated>
  </verify>
  <done>setup_game correctly distributes territories round-robin and places armies per classic Risk rules for all valid player counts (2-6). All tests pass including army count totals, minimum-1-army constraint, and deterministic seeded behavior. Full test suite green.</done>
</task>

</tasks>

<verification>
- `pytest tests/ -v` -- all tests pass (map data, graph, game state, setup)
- Territory distribution is balanced (max 1 territory difference)
- Army totals match classic starting counts for all player counts 2-6
- Every territory has at least 1 army after setup
</verification>

<success_criteria>
1. Territories are randomly distributed among N players with at most 1 territory difference between any two players
2. Every territory has at least 1 army after setup
3. Total armies per player equal the classic starting count (2p=40, 3p=35, 4p=30, 5p=25, 6p=20)
4. Game state models correctly validate and serialize
5. Full test suite passes: `pytest tests/ -v`
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>

---
phase: 03-web-ui-and-game-setup
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - risk/server/game_manager.py
  - risk/server/human_agent.py
  - risk/server/app.py
  - risk/static/app.js
  - tests/test_server.py
autonomous: false
requirements: [SETUP-01, MAPV-02, MAPV-03, MAPV-04, MAPV-05, MAPV-06, MAPV-07]

must_haves:
  truths:
    - "A human player can start a new game by selecting player count and clicking Start"
    - "The game runs end-to-end: setup -> reinforcement -> attack -> fortify -> bot turns -> back to human"
    - "Territory colors and army counts update in real time as the game progresses"
    - "Game log shows all key events throughout the game"
    - "Game ends with win/loss screen and New Game button works"
  artifacts:
    - path: "tests/test_server.py"
      provides: "Integration tests for WebSocket game flow"
      min_lines: 50
  key_links:
    - from: "risk/static/app.js"
      to: "risk/server/app.py"
      via: "Full WebSocket game loop: start -> state -> input -> action -> state -> ..."
      pattern: "start_game|game_state|request_input|player_action"
    - from: "risk/server/game_manager.py"
      to: "risk/engine/turn.py"
      via: "execute_turn called in background thread per player turn"
      pattern: "execute_turn"
---

<objective>
Integration testing and end-to-end wiring of the complete Risk web game. Fix any gaps between the server and frontend, add integration tests, and verify the full game loop works in a browser.

Purpose: Plans 01-03 built the pieces independently. This plan connects them, resolves any interface mismatches, and validates the complete player experience through both automated tests and human verification.

Output: Working end-to-end game, integration test suite, and human-verified gameplay.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-web-ui-and-game-setup/03-CONTEXT.md
@.planning/phases/03-web-ui-and-game-setup/03-RESEARCH.md
@.planning/phases/03-web-ui-and-game-setup/03-01-SUMMARY.md
@.planning/phases/03-web-ui-and-game-setup/03-02-SUMMARY.md
@.planning/phases/03-web-ui-and-game-setup/03-03-SUMMARY.md

@risk/server/messages.py
@risk/server/human_agent.py
@risk/server/game_manager.py
@risk/server/app.py
@risk/static/app.js
@risk/static/map.js
@risk/static/sidebar.js
@risk/static/index.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests and end-to-end wiring fixes</name>
  <files>tests/test_server.py, risk/server/game_manager.py, risk/server/human_agent.py, risk/server/app.py, risk/static/app.js</files>
  <behavior>
    - Test: WebSocket connect to /ws succeeds
    - Test: Sending start_game with num_players=3 results in a game_state message with 3 players and 42 territories
    - Test: Game state includes territory owners (0-2) and army counts (>= 1) -- validates MAPV-02, MAPV-03
    - Test: Game state includes turn phase and current player index -- validates MAPV-05
    - Test: Game state includes continent info with bonus values -- validates MAPV-07
    - Test: After start_game, server sends request_input for human's first turn (choose_card_trade or choose_reinforcement_placement)
    - Test: Sending a valid reinforcement placement action results in updated game state transitioning to attack phase
    - Test: Game events are emitted during bot turns -- validates MAPV-06
    - Test: Player action messages are correctly parsed into game actions -- validates MAPV-04
  </behavior>
  <action>
    1. Create `tests/test_server.py` with integration tests using FastAPI's TestClient with WebSocket support (httpx):

    ```python
    import pytest
    from fastapi.testclient import TestClient
    from risk.server.app import app

    # Test WebSocket connection
    # Test start_game creates correct game
    # Test game state message format (owners, armies, phase, continent info)
    # Test request_input for human turn
    # Test reinforcement action handling
    # Test game event emission
    ```

    Use `with client.websocket_connect("/ws") as ws:` pattern. Send start_game, receive messages, validate structure.

    Note: The TestClient WebSocket is synchronous. The game loop runs in a thread. Tests need to handle the async nature -- send start_game, then receive multiple messages (game_state, request_input) and validate them.

    2. Fix any integration issues discovered while writing tests:
    - Ensure message format consistency between server and client (field names, enum serialization)
    - Ensure GameManager correctly sends game_state after each turn
    - Ensure HumanWebSocketAgent correctly sends request_input with valid sources/targets
    - Ensure bot turn delays work (0.5s per user decision -- may need to reduce for tests)
    - Add a `bot_delay` parameter to GameManager so tests can set it to 0

    3. Fix any frontend JS issues:
    - Verify app.js correctly parses all server message formats
    - Verify map.js handles the actual GameState territory structure (check field names match)
    - Verify sidebar.js correctly reads continent_info from game_state messages
    - Ensure the SVG map loads correctly from the /api/map route

    4. Ensure the full flow works:
    - Start game -> receive initial state -> receive request_input for reinforcement
    - Send reinforcement -> receive updated state (attack phase) -> receive request_input for attack
    - Send end_phase -> receive state (fortify phase) -> receive request_input for fortify
    - Send end_phase -> bot turns execute -> receive game events -> receive state for next human turn
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk && pytest tests/test_server.py -x --timeout=30</automated>
  </verify>
  <done>Integration tests pass confirming: WebSocket connection works, game creates with correct players, state includes owners/armies/phase/continents, human input request/response cycle works, bot turns emit events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete game experience</name>
  <files>none</files>
  <action>
    Start the server and verify the complete game experience in a browser. This checkpoint confirms that all visual and interactive elements work as designed -- things that automated tests cannot verify (SVG rendering, click interactions, visual layout, real-time updates during bot turns).

    Start command: `cd /home/amiller/Repos/risk && python -m uvicorn risk.server.app:app --reload --port 8000`
    URL: http://localhost:8000

    Verification steps:
    1. Verify setup screen: select 3 players, click "Start Game"
    2. Verify map appears with 42 territories colored by owner (3 colors) and army counts visible
    3. Verify sidebar shows: turn phase stepper (Reinforce highlighted), continent bonuses with territory counts, empty game log
    4. Verify phase banner shows "Your Turn: Place N reinforcements"
    5. Click a territory you own -- verify reinforcement number input appears
    6. Place all reinforcements across your territories
    7. Verify phase transitions to Attack, banner updates
    8. Click a source territory (yours with 2+ armies) -- verify it highlights, valid enemy targets glow
    9. Click an enemy neighbor -- verify attack executes, dice results in game log
    10. Click "End Attack Phase" -- verify phase moves to Fortify
    11. Click "Skip Fortify" or perform a fortify move
    12. Verify bot turns execute with visible delays (~500ms), map updates, game log shows bot actions
    13. Verify game continues cycling through turns
  </action>
  <verify>Human confirms game is playable by completing the verification steps above</verify>
  <done>Human has verified: setup works, map renders with colors and armies, click-click interaction works, sidebar updates correctly, game log shows events, bot turns are visible. Game is playable end-to-end.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_server.py -x --timeout=30` integration tests pass
- `pytest tests/ --timeout=30` full suite green
- Human verifies game is playable in browser
</verification>

<success_criteria>
- All 7 phase requirements (SETUP-01, MAPV-02-07) are demonstrably working
- A human can play a complete Risk game against bots in the browser
- Game log captures all key events
- Bot turns are visible with delays
- New Game button returns to setup screen
</success_criteria>

<output>
After completion, create `.planning/phases/03-web-ui-and-game-setup/03-04-SUMMARY.md`
</output>

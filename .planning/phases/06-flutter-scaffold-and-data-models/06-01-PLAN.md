---
phase: 06-flutter-scaffold-and-data-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/pubspec.yaml
  - mobile/analysis_options.yaml
  - mobile/build.yaml
  - mobile/lib/main.dart
  - mobile/lib/app.dart
  - mobile/lib/screens/home_screen.dart
  - mobile/assets/classic.json
  - mobile/test/engine/map_graph_test.dart
  - mobile/test/engine/models_test.dart
  - mobile/test/persistence/save_slot_test.dart
autonomous: true
requirements:
  - DART-07

must_haves:
  truths:
    - "flutter pub get succeeds with no errors"
    - "dart run build_runner build --delete-conflicting-outputs exits cleanly (no code to generate yet, just verifies toolchain)"
    - "Test stub files exist and fail cleanly (no import errors, just 'not yet implemented')"
    - "classic.json is present in mobile/assets/ with 42 territories"
    - "analysis_options.yaml suppresses invalid_annotation_target"
    - "build.yaml enables explicit_to_json"
  artifacts:
    - path: "mobile/pubspec.yaml"
      provides: "All dependency versions pinned correctly per research"
    - path: "mobile/build.yaml"
      provides: "explicit_to_json: true for nested freezed serialization"
    - path: "mobile/analysis_options.yaml"
      provides: "invalid_annotation_target: ignore for freezed annotations"
    - path: "mobile/assets/classic.json"
      provides: "42-territory map data as Flutter asset"
    - path: "mobile/test/engine/map_graph_test.dart"
      provides: "MapGraph test stubs (fail until Wave 2)"
    - path: "mobile/test/engine/models_test.dart"
      provides: "Freezed model test stubs (fail until Wave 2)"
    - path: "mobile/test/persistence/save_slot_test.dart"
      provides: "ObjectBox round-trip test stub (fail until Plan 03)"
  key_links:
    - from: "mobile/pubspec.yaml flutter.assets"
      to: "mobile/assets/classic.json"
      via: "flutter asset declaration"
      pattern: "assets/classic.json"
    - from: "mobile/lib/main.dart"
      to: "ProviderScope"
      via: "runApp(ProviderScope(...))"
      pattern: "ProviderScope"
---

<objective>
Create the Flutter project scaffold for `risk_mobile` with all dependencies, configuration files, placeholder source files, and test stubs. This plan delivers the empty-but-correct foundation that Plans 02 and 03 build on.

Purpose: Getting pubspec.yaml, build.yaml, and analysis_options.yaml right on the first try is the most common failure mode for Flutter code-gen phases. Isolating this work in Wave 1 lets Plans 02 and 03 proceed in parallel with confidence.
Output: A `mobile/` subdirectory that compiles, passes `flutter pub get`, and has all test stub files in place.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-flutter-scaffold-and-data-models/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Flutter project and configure dependencies</name>
  <files>
    mobile/pubspec.yaml
    mobile/analysis_options.yaml
    mobile/build.yaml
    mobile/lib/main.dart
    mobile/lib/app.dart
    mobile/lib/screens/home_screen.dart
    mobile/assets/classic.json
  </files>
  <action>
From the repo root `/home/amiller/Repos/risk`, run:

```bash
flutter create --org com.riskgame --project-name risk_mobile mobile
```

This generates the project skeleton. Then replace the generated `mobile/pubspec.yaml` `dependencies` and `dev_dependencies` sections with the exact versions from research. Do NOT use `flutter pub add` commands — write pubspec.yaml directly to avoid version drift.

**pubspec.yaml environment section:**
```yaml
environment:
  sdk: ">=3.7.0 <4.0.0"
  flutter: ">=3.41.0"
```

**pubspec.yaml dependencies:**
```yaml
dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.3.1
  riverpod_annotation: ^4.0.2
  freezed_annotation: ^3.0.0
  json_annotation: ^4.9.0
  objectbox: ^5.2.0
  objectbox_flutter_libs: any
  shared_preferences: ^2.5.4
  path_parsing: ^1.1.0
  flutter_svg: ^2.2.4
```

**pubspec.yaml dev_dependencies (keep flutter_test SDK dep, add these):**
```yaml
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  riverpod_generator: ^4.0.3
  freezed: ^3.2.5
  json_serializable: ^6.13.0
  objectbox_generator: any
  mocktail: ^1.0.4
```

**pubspec.yaml flutter assets section:**
```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/classic.json
```

**analysis_options.yaml** (write at `mobile/analysis_options.yaml`):
```yaml
include: package:flutter_lints/flutter.yaml

analyzer:
  errors:
    invalid_annotation_target: ignore
```

**build.yaml** (write at `mobile/build.yaml`):
```yaml
targets:
  $default:
    builders:
      json_serializable:
        options:
          explicit_to_json: true
```

**Copy the map asset:**
Copy `risk/data/classic.json` to `mobile/assets/classic.json`. Create the `mobile/assets/` directory first.

**Minimal lib/main.dart** (replace generated content):
```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'app.dart';

void main() {
  WidgetsFlutterBinding.ensureInitialized();
  runApp(
    const ProviderScope(
      child: RiskApp(),
    ),
  );
}
```

**lib/app.dart:**
```dart
import 'package:flutter/material.dart';
import 'screens/home_screen.dart';

class RiskApp extends StatelessWidget {
  const RiskApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Risk',
      theme: ThemeData(colorSchemeSeed: Colors.red),
      home: const HomeScreen(),
    );
  }
}
```

**lib/screens/home_screen.dart:**
```dart
import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Risk Mobile — Coming Soon')),
    );
  }
}
```

Run `cd mobile && flutter pub get` to verify dependencies resolve. Fix any version conflicts before proceeding.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && flutter pub get 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    `flutter pub get` exits 0 with no errors. `mobile/pubspec.yaml` contains all listed dependencies. `mobile/assets/classic.json` exists. `mobile/build.yaml` and `mobile/analysis_options.yaml` are written with the required content.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write test stubs (Wave 0 contracts)</name>
  <files>
    mobile/test/engine/map_graph_test.dart
    mobile/test/engine/models_test.dart
    mobile/test/persistence/save_slot_test.dart
  </files>
  <behavior>
    - map_graph_test.dart: group 'adjacency' with tests for 42-territory count, bidirectional edges, non-adjacent pairs, Alaska neighbors (3: Northwest Territory, Alberta, Kamchatka); group 'BFS connected territories' with isolated-start and full-South-America tests; group 'continent queries' with Australia bonus=2 and controlsContinent=true for all 4 Australia territories
    - models_test.dart: group 'TerritoryState' with copyWith changes owner/armies, equality holds for same values; group 'GameState' with JSON round-trip (toJson then fromJson produces equal object)
    - save_slot_test.dart: test 'write and read SaveSlot' that writes a SaveSlot with gameStateJson='{"test":true}' and reads it back with matching value
  </behavior>
  <action>
Create the test directories and write stub tests. These tests MUST FAIL at this stage (the classes they import don't exist yet). They will turn green in Plans 02 and 03.

**mobile/test/engine/map_graph_test.dart:**
Write a test file with `import 'package:risk_mobile/engine/map_graph.dart'` and `import 'package:risk_mobile/engine/models/map_schema.dart'`. Include a `const Map classicMapJson` fixture with the classic map data (copy the JSON structure inline — do NOT use rootBundle in unit tests). Define all 8 test cases from the behavior block.

For the inline map fixture, embed the full `classic.json` content as a Dart `const` string literal: `final classicJson = '...json string...'` loaded with `jsonDecode(classicJson)`. Read `risk/data/classic.json` and embed it.

**mobile/test/engine/models_test.dart:**
Write a test file with `import 'package:risk_mobile/engine/models/game_state.dart'` and `import 'package:risk_mobile/engine/models/cards.dart'`. Include test cases for TerritoryState copyWith, PlayerState equality, and GameState JSON round-trip.

**mobile/test/persistence/save_slot_test.dart:**
Write a test file with `import 'package:risk_mobile/persistence/save_slot.dart'`. Note: ObjectBox integration tests require device/emulator; mark with `@Skip('Requires ObjectBox native libs — run with flutter test not dart test')` so they don't block pure-Dart CI. The test body still verifies the SaveSlot class and its fields exist.

Run `cd mobile && dart test test/engine/` — expect compilation errors (missing imports) until Plan 02 completes. This is intentional; the stubs define the contract.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && dart test test/engine/ 2>&amp;1 | head -20</automated>
  </verify>
  <done>
    Test files exist at the correct paths. Running `dart test` shows compilation errors (missing imports) or test failures — NOT syntax errors in the test files themselves. The test file structure is valid Dart.
  </done>
</task>

</tasks>

<verification>
1. `flutter pub get` exits 0 from `mobile/` directory
2. `mobile/pubspec.yaml` contains `riverpod_generator: ^4.0.3` (NOT 2.6.0) and `objectbox_flutter_libs: any` (NOT path dependency)
3. `mobile/build.yaml` contains `explicit_to_json: true`
4. `mobile/analysis_options.yaml` contains `invalid_annotation_target: ignore`
5. `mobile/assets/classic.json` exists and contains 42 territories
6. All 3 test stub files exist under `mobile/test/`
</verification>

<success_criteria>
- `cd mobile && flutter pub get` succeeds
- `mobile/assets/classic.json` has 42 territories (same as `risk/data/classic.json`)
- `mobile/build.yaml` and `mobile/analysis_options.yaml` match research spec
- All test stub files exist at correct paths (they will fail on imports — that is expected)
</success_criteria>

<output>
After completion, create `.planning/phases/06-flutter-scaffold-and-data-models/06-01-SUMMARY.md`
</output>

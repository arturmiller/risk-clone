---
phase: 11-screens-widgets-and-mobile-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/test/screens/home_screen_test.dart
  - mobile/test/engine/game_config_test.dart
  - mobile/test/screens/game_screen_test.dart
  - mobile/test/widgets/action_panel_test.dart
  - mobile/test/providers/human_move_test.dart
  - mobile/test/widgets/game_log_test.dart
  - mobile/test/widgets/continent_panel_test.dart
  - mobile/test/widgets/game_over_dialog_test.dart
autonomous: true
requirements:
  - MOBX-01
  - MOBX-02
  - MOBX-03
  - MOBX-04
  - MOBX-05
  - MOBX-06

must_haves:
  truths:
    - "All 8 test stub files exist and compile cleanly"
    - "flutter test runs all stub tests without analyzer errors"
    - "Each stub test file contains markTestSkipped() calls (not skip: parameter)"
  artifacts:
    - path: "mobile/test/screens/home_screen_test.dart"
      provides: "MOBX-01 HomeScreen setup form test scaffolding"
    - path: "mobile/test/engine/game_config_test.dart"
      provides: "MOBX-01 GameMode enum and gameMode field test scaffolding"
    - path: "mobile/test/screens/game_screen_test.dart"
      provides: "MOBX-02 portrait/landscape layout test scaffolding"
    - path: "mobile/test/widgets/action_panel_test.dart"
      provides: "MOBX-03 phase-aware ActionPanel test scaffolding"
    - path: "mobile/test/providers/human_move_test.dart"
      provides: "MOBX-03 humanMove() GameNotifier test scaffolding"
    - path: "mobile/test/widgets/game_log_test.dart"
      provides: "MOBX-04 GameLog widget test scaffolding"
    - path: "mobile/test/widgets/continent_panel_test.dart"
      provides: "MOBX-05 ContinentPanel test scaffolding"
    - path: "mobile/test/widgets/game_over_dialog_test.dart"
      provides: "MOBX-06 GameOverDialog test scaffolding"
  key_links:
    - from: "test stubs"
      to: "implementation tasks"
      via: "markTestSkipped — compile OK, fail when uncommented"
      pattern: "markTestSkipped"
---

<objective>
Create Wave 0 test stub files for all Phase 11 components. Every subsequent implementation task has an automated verify command referencing these files.

Purpose: Nyquist compliance — no implementation task can have `MISSING` in its verify command. All 8 test files must compile before any production code is written.
Output: 8 compilable test stubs with `markTestSkipped('not yet implemented')` calls in each test body.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@mobile/lib/engine/models/game_config.dart
@mobile/lib/engine/models/game_state.dart
@mobile/lib/screens/home_screen.dart
@mobile/lib/providers/game_provider.dart
@mobile/lib/providers/ui_provider.dart
@mobile/test/widgets/map/map_widget_test.dart

<interfaces>
<!-- Existing patterns to follow in test stubs -->

From mobile/test/widgets/map/map_widget_test.dart (Phase 10 pattern):
```dart
// testWidgets uses markTestSkipped in body — bool? constraint (STATE.md [10-01])
testWidgets('description', (tester) async {
  markTestSkipped('not yet implemented');
});
```

From mobile/test/providers/game_notifier_test.dart (Phase 9 pattern):
```dart
// Widget tests wrap with ProviderScope
await tester.pumpWidget(ProviderScope(child: widget));
// Provider tests use ProviderContainer with overrides
```

From mobile/lib/engine/models/game_state.dart:
```dart
// TurnPhase enum values used in ActionPanel tests:
enum TurnPhase { reinforce, attack, fortify }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create screen test stubs (HomeScreen + GameScreen)</name>
  <files>mobile/test/screens/home_screen_test.dart, mobile/test/engine/game_config_test.dart, mobile/test/screens/game_screen_test.dart</files>
  <action>
Create three stub test files. Each must compile cleanly without importing files that don't exist yet.

`mobile/test/screens/home_screen_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('HomeScreen setup form (MOBX-01)', () {
    testWidgets('renders player count selector', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    testWidgets('renders difficulty selector', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    testWidgets('renders game mode selector', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    testWidgets('tapping Start calls setupGame with chosen config', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    testWidgets('Resume button navigates to GameScreen when game in progress', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });
  });
}
```

`mobile/test/engine/game_config_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
// ignore_for_file: unused_import

void main() {
  group('GameConfig + GameMode (MOBX-01)', () {
    test('GameMode enum has vsBot and simulation values', () {
      markTestSkipped('not yet implemented — Phase 11 Plan 02');
    });

    test('GameConfig defaults gameMode to vsBot', () {
      markTestSkipped('not yet implemented — Phase 11 Plan 02');
    });

    test('GameConfig accepts explicit gameMode', () {
      markTestSkipped('not yet implemented — Phase 11 Plan 02');
    });
  });
}
```

`mobile/test/screens/game_screen_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('GameScreen layout (MOBX-02)', () {
    testWidgets('portrait layout: map above bottom panel, no overflow', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 06');
    });

    testWidgets('landscape layout (600dp+ width): map left, sidebar right', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 06');
    });

    testWidgets('PopScope shows abandon dialog on back press', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 06');
    });
  });
}
```
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test test/screens/home_screen_test.dart test/engine/game_config_test.dart test/screens/game_screen_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>All three files exist and all tests pass (as skipped). No analyzer errors. `flutter test` reports 0 failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create widget + provider test stubs (ActionPanel, humanMove, GameLog, ContinentPanel, GameOverDialog)</name>
  <files>mobile/test/widgets/action_panel_test.dart, mobile/test/providers/human_move_test.dart, mobile/test/widgets/game_log_test.dart, mobile/test/widgets/continent_panel_test.dart, mobile/test/widgets/game_over_dialog_test.dart</files>
  <action>
Create five stub test files. All must compile without importing Phase 11 files that do not yet exist.

`mobile/test/widgets/action_panel_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('ActionPanel (MOBX-03)', () {
    testWidgets('shows reinforce controls when turnPhase == reinforce', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });

    testWidgets('shows attack controls (dice selector, end attack) when turnPhase == attack', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });

    testWidgets('shows fortify controls (skip fortify) when turnPhase == fortify', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });

    testWidgets('Blitz button dispatches BlitzAction via humanMove', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });
  });
}
```

`mobile/test/providers/human_move_test.dart`:
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('GameNotifier.humanMove (MOBX-03)', () {
    test('humanMove reinforce places armies and transitions to attack phase', () async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    test('humanMove attack with AttackAction executes combat step', () async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    test('humanMove fortify moves armies and transitions to next player', () async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });

    test('humanMove null attack (end attack) transitions to fortify phase', () async {
      markTestSkipped('not yet implemented — Phase 11 Plan 03');
    });
  });
}
```

`mobile/test/widgets/game_log_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('GameLog widget (MOBX-04)', () {
    testWidgets('renders empty state with no entries', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });

    testWidgets('renders list of LogEntry messages', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 04');
    });

    test('gameLogProvider accumulates entries via add()', () {
      markTestSkipped('not yet implemented — Phase 11 Plan 02');
    });

    test('gameLogProvider resets to empty via clear()', () {
      markTestSkipped('not yet implemented — Phase 11 Plan 02');
    });
  });
}
```

`mobile/test/widgets/continent_panel_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('ContinentPanel (MOBX-05)', () {
    testWidgets('renders all continent names', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });

    testWidgets('renders continent bonus values', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });

    testWidgets('highlights controlled continent for current player', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });
  });
}
```

`mobile/test/widgets/game_over_dialog_test.dart`:
```dart
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';

// ignore_for_file: unused_import

void main() {
  group('GameOverDialog (MOBX-06)', () {
    testWidgets('shows winner name', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });

    testWidgets('New Game button triggers clearSave', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });

    testWidgets('Home button navigates back to HomeScreen', (tester) async {
      markTestSkipped('not yet implemented — Phase 11 Plan 05');
    });
  });
}
```
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test test/widgets/action_panel_test.dart test/providers/human_move_test.dart test/widgets/game_log_test.dart test/widgets/continent_panel_test.dart test/widgets/game_over_dialog_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>All five files exist and all tests pass (as skipped). No analyzer errors.</done>
</task>

</tasks>

<verification>
All 8 test stub files compile and run: `cd mobile && flutter test test/screens/ test/engine/game_config_test.dart test/widgets/action_panel_test.dart test/providers/human_move_test.dart test/widgets/game_log_test.dart test/widgets/continent_panel_test.dart test/widgets/game_over_dialog_test.dart`

Every test should be "skipped" — zero failures, zero errors.
</verification>

<success_criteria>
8 test files exist in the correct directories. `flutter test` reports all tests as skipped (not failed). The full test suite (`flutter test`) still shows 168 tests passing + the new stubs as skipped.
</success_criteria>

<output>
After completion, create `.planning/phases/11-screens-widgets-and-mobile-ux/11-01-SUMMARY.md`
</output>

---
phase: 11-screens-widgets-and-mobile-ux
plan: 07
type: execute
wave: 6
depends_on:
  - 11-06
files_modified: []
autonomous: false
requirements:
  - MOBX-01
  - MOBX-02
  - MOBX-03
  - MOBX-04
  - MOBX-05
  - MOBX-06

must_haves:
  truths:
    - "User can configure player count, difficulty, and game mode from HomeScreen and start a game"
    - "Game is playable in portrait mode (map + bottom panel) without layout overflow"
    - "Game is playable in landscape mode (map + sidebar) without hidden controls"
    - "All game actions — reinforce, attack, blitz, end attack, skip fortify — execute correctly"
    - "Game log updates with readable events during gameplay"
    - "Continent panel shows correct bonuses for current player"
    - "Game over dialog appears when a player wins, offers new game option"
  artifacts:
    - path: "mobile/lib/screens/game_screen.dart"
      provides: "Complete game screen — verified by human playthrough"
    - path: "mobile/lib/widgets/action_panel.dart"
      provides: "Phase-aware controls — verified by human playthrough"
  key_links:
    - from: "HomeScreen Start button"
      to: "GameScreen navigation"
      via: "Navigator.push after setupGame()"
      pattern: "Navigator.push"
    - from: "GameScreen"
      to: "GameOverDialog"
      via: "ref.listen + addPostFrameCallback on single-player-alive condition"
      pattern: "GameOverDialog"
---

<objective>
Human verification of the complete game UI. Claude runs the app on a connected device or emulator; the user plays a short game from setup through game-over and confirms all Phase 11 requirements are met.

Purpose: Automated tests verify individual widget behavior but cannot confirm the end-to-end game feel, layout quality, and interaction correctness that require human judgment.
Output: Confirmed working game UI or a list of issues to address in gap-closure plans.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and flutter analyze</name>
  <files></files>
  <action>
Before human verification, ensure the codebase is clean:

1. Run full test suite:
```bash
cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test
```

2. Run static analysis:
```bash
cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter analyze
```

Both must pass with 0 errors before proceeding to the human-verify checkpoint. If any failures exist, fix them before requesting human verification.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test 2>&1 | tail -5 && /home/amiller/flutter-sdk/flutter/bin/flutter analyze 2>&1 | tail -5</automated>
  </verify>
  <done>All tests pass. `flutter analyze` reports 0 errors (warnings are acceptable).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify complete game UI (MOBX-01 through MOBX-06)</name>
  <action>Run the app: `cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter run` and play through the full game flow per the verification steps below.</action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test 2>&1 | tail -3</automated>
  </verify>
  <done>User types "approved" after completing all 30 verification steps.</done>

  <what-built>
Complete Phase 11 game UI:
- HomeScreen setup form (player count 2-6, difficulty Easy/Medium/Hard, mode vs Bots/Simulation)
- GameScreen with portrait layout (map + bottom ActionPanel) and landscape layout (map + sidebar with ActionPanel/GameLog/ContinentPanel)
- ActionPanel phase-aware controls: reinforce army placement, attack dice selector + Blitz, fortify slider + Skip
- GameLog showing attack/conquest events in real time
- ContinentPanel showing all continents with bonuses and controlled-star indicators
- GameOverDialog when a player wins, with Home/New Game options
- PopScope abandon-game dialog on back press
- HumanAgent wiring: humanMove() on main isolate, auto bot turns via _advanceTurnIfBot()
  </what-built>
  <how-to-verify>
Run the app on a connected Android device or emulator. Flutter SDK is at `/home/amiller/flutter-sdk/flutter`:

```bash
cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter run
```

Verify each requirement:

**MOBX-01 (Setup screen):**
1. App opens to HomeScreen
2. Adjust player count slider — confirm it changes between 2 and 6
3. Tap "Hard" difficulty — confirm segment highlights
4. Tap "Simulation" mode — confirm segment highlights
5. Tap "Start Game" — confirm navigation to GameScreen

**MOBX-02 (Responsive layout):**
6. In portrait orientation: confirm map fills top, action controls visible at bottom, no overflow errors
7. Rotate to landscape: confirm map on left, sidebar (actions, log, continents) on right
8. Confirm no "RenderFlex overflowed" yellow/red error banners

**MOBX-03 (Game action controls):**
9. In reinforce phase: confirm armies-remaining count is shown
10. Tap a territory you own — confirm it's selected on map
11. Tap "Confirm Placement" when all armies placed — confirm phase advances to attack
12. In attack phase: confirm dice selector (1/2/3) and "End Attack" button visible
13. Select source territory, select enemy target, tap "Attack" — confirm combat executes
14. Tap "Blitz" with valid source+target — confirm blitz executes to conquest or failure
15. Tap "End Attack" — confirm phase advances to fortify
16. In fortify phase: confirm "Skip Fortify" button visible
17. Tap "Skip Fortify" — confirm turn advances to next player (bot turn runs automatically)

**MOBX-04 (Game log):**
18. After each significant event (attack, conquest), confirm a new entry appears in game log
19. Confirm log scrolls to show latest entry automatically

**MOBX-05 (Continent panel — landscape only, or in sidebar):**
20. Confirm all 6 continent names visible: North America, South America, Europe, Africa, Asia, Australia
21. Confirm bonus values shown (e.g. "+5" for North America)
22. Confirm a star icon appears next to continent when current player controls it

**MOBX-06 (Game over):**
23. Play until one player wins (or use 2-player Hard mode to speed up)
24. Confirm GameOverDialog appears with "[Player name] wins!"
25. Tap "New Game" — confirm dialog dismisses and app returns to HomeScreen
26. Confirm HomeScreen shows "No active game" state (setup form, not resume)

**Back navigation:**
27. During a game, press Android back button or swipe back on iOS
28. Confirm "Abandon game?" dialog appears
29. Tap Cancel — confirm stays in game
30. Tap Abandon — confirm returns to HomeScreen
  </how-to-verify>
  <resume-signal>
Type "approved" if all 30 verification steps pass.

If issues found, describe them specifically (e.g. "Step 7: overflow banner in landscape mode" or "Step 18: game log not updating"). Gap-closure plans will be created to address them.
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms all 6 MOBX requirements verified via hands-on testing. All automated tests still green.
</verification>

<success_criteria>
- All 30 verification steps pass
- No "RenderFlex overflowed" errors in either orientation
- Game loop completes: setup → play → game over → new game
- Bot turns run automatically after human turns without UI freezing
- User types "approved" to signal Phase 11 complete
</success_criteria>

<output>
After completion, create `.planning/phases/11-screens-widgets-and-mobile-ux/11-07-SUMMARY.md`
</output>

---
phase: 10-map-widget
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mobile/lib/widgets/map/territory_data.dart
  - mobile/test/widgets/map/map_widget_test.dart
  - mobile/test/widgets/map/map_interaction_test.dart
autonomous: true
requirements:
  - MAPW-03
user_setup: []

must_haves:
  truths:
    - "All 42 territories have Rect and label Offset constants in TerritoryGeometry"
    - "kTerritoryData keys match GameState.territories keys exactly"
    - "kPlayerColors defines 6 player colors"
    - "Wave 0 test stubs exist and compile without errors (tests skip/fail intentionally)"
  artifacts:
    - path: "mobile/lib/widgets/map/territory_data.dart"
      provides: "42 TerritoryGeometry constants + kPlayerColors"
      exports: ["kTerritoryData", "TerritoryGeometry", "kPlayerColors"]
    - path: "mobile/test/widgets/map/map_widget_test.dart"
      provides: "Widget test stubs for MAPW-01 and MAPW-03"
    - path: "mobile/test/widgets/map/map_interaction_test.dart"
      provides: "Widget test stubs for MAPW-02, MAPW-04, MAPW-05"
  key_links:
    - from: "mobile/lib/widgets/map/territory_data.dart"
      to: "GameState.territories"
      via: "kTerritoryData keys must match territory name strings"
      pattern: "kTerritoryData"
---

<objective>
Define the static territory geometry data (all 42 Rect constants + label positions) and
player color palette that every subsequent painter and hit-test depends on. Also create
Wave 0 test stubs so automated verification commands exist from the start.

Purpose: Establishing `territory_data.dart` first prevents the scavenger hunt anti-pattern
where painters and widget code have to discover geometry. All downstream tasks receive
contracts, not blanks to fill in.

Output: `territory_data.dart` with `kTerritoryData` and `kPlayerColors`, two test stub
files that compile cleanly and fail with skip/todo markers.
</objective>

<execution_context>
@/home/amiller/.claude/get-shit-done/workflows/execute-plan.md
@/home/amiller/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-map-widget/10-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create territory_data.dart with all 42 TerritoryGeometry constants</name>
  <files>mobile/lib/widgets/map/territory_data.dart</files>
  <action>
Create `mobile/lib/widgets/map/territory_data.dart` with:

1. A `TerritoryGeometry` class (const-constructible, no annotation needed):
```dart
import 'dart:ui';

class TerritoryGeometry {
  final Rect rect;
  final Offset labelOffset;
  const TerritoryGeometry({required this.rect, required this.labelOffset});
}
```

2. `kPlayerColors` — a const List of 6 Colors (one per player index 0-5):
- P0: Color(0xFFE53935) — Red
- P1: Color(0xFF1E88E5) — Blue
- P2: Color(0xFF43A047) — Green
- P3: Color(0xFFFDD835) — Yellow
- P4: Color(0xFF8E24AA) — Purple
- P5: Color(0xFFFF7043) — Orange

3. `kTerritoryData` — a const Map<String, TerritoryGeometry> with all 42 territories
   using EXACTLY these Rect.fromLTWH(left, top, width, height) values and label Offsets
   from the SVG coordinate space (viewBox 0 0 1200 700):

Alaska: Rect.fromLTWH(30, 60, 60, 50), label: Offset(60, 85)
Northwest Territory: Rect.fromLTWH(100, 45, 100, 50), label: Offset(150, 70)
Greenland: Rect.fromLTWH(260, 30, 90, 60), label: Offset(305, 60)
Alberta: Rect.fromLTWH(70, 120, 70, 55), label: Offset(105, 147)
Ontario: Rect.fromLTWH(150, 105, 80, 65), label: Offset(190, 137)
Quebec: Rect.fromLTWH(240, 100, 70, 65), label: Offset(275, 132)
Western United States: Rect.fromLTWH(60, 185, 85, 65), label: Offset(102, 217)
Eastern United States: Rect.fromLTWH(155, 175, 95, 80), label: Offset(202, 215)
Central America: Rect.fromLTWH(100, 260, 85, 65), label: Offset(142, 292)
Venezuela: Rect.fromLTWH(160, 340, 95, 55), label: Offset(207, 367)
Peru: Rect.fromLTWH(145, 405, 75, 70), label: Offset(182, 440)
Brazil: Rect.fromLTWH(230, 390, 90, 80), label: Offset(275, 430)
Argentina: Rect.fromLTWH(170, 485, 90, 85), label: Offset(215, 527)
North Africa: Rect.fromLTWH(400, 295, 110, 80), label: Offset(455, 335)
Egypt: Rect.fromLTWH(520, 285, 90, 65), label: Offset(565, 317)
East Africa: Rect.fromLTWH(545, 360, 85, 85), label: Offset(587, 402)
Congo: Rect.fromLTWH(460, 385, 75, 75), label: Offset(497, 422)
South Africa: Rect.fromLTWH(480, 470, 95, 85), label: Offset(527, 512)
Madagascar: Rect.fromLTWH(620, 470, 60, 70), label: Offset(650, 505)
Iceland: Rect.fromLTWH(400, 65, 60, 45), label: Offset(430, 87)
Scandinavia: Rect.fromLTWH(500, 60, 75, 65), label: Offset(537, 92)
Ukraine: Rect.fromLTWH(585, 70, 95, 105), label: Offset(632, 122)
Great Britain: Rect.fromLTWH(405, 125, 70, 60), label: Offset(440, 155)
Northern Europe: Rect.fromLTWH(485, 135, 90, 60), label: Offset(530, 165)
Southern Europe: Rect.fromLTWH(490, 205, 90, 65), label: Offset(535, 237)
Western Europe: Rect.fromLTWH(400, 200, 80, 80), label: Offset(440, 240)
Ural: Rect.fromLTWH(700, 60, 70, 80), label: Offset(735, 100)
Siberia: Rect.fromLTWH(780, 45, 70, 80), label: Offset(815, 85)
Yakutsk: Rect.fromLTWH(860, 40, 85, 60), label: Offset(902, 70)
Kamchatka: Rect.fromLTWH(955, 35, 85, 70), label: Offset(997, 70)
Irkutsk: Rect.fromLTWH(870, 110, 80, 60), label: Offset(910, 140)
Mongolia: Rect.fromLTWH(880, 180, 95, 60), label: Offset(927, 210)
Japan: Rect.fromLTWH(1000, 150, 60, 70), label: Offset(1030, 185)
Afghanistan: Rect.fromLTWH(700, 150, 90, 65), label: Offset(745, 182)
China: Rect.fromLTWH(800, 180, 70, 80), label: Offset(835, 220)
Middle East: Rect.fromLTWH(620, 195, 100, 85), label: Offset(670, 237)
India: Rect.fromLTWH(750, 225, 80, 85), label: Offset(790, 267)
Siam: Rect.fromLTWH(840, 270, 70, 75), label: Offset(875, 307)
Indonesia: Rect.fromLTWH(880, 385, 80, 60), label: Offset(920, 415)
New Guinea: Rect.fromLTWH(1000, 380, 85, 50), label: Offset(1042, 405)
Western Australia: Rect.fromLTWH(900, 460, 85, 80), label: Offset(942, 500)
Eastern Australia: Rect.fromLTWH(1010, 445, 85, 95), label: Offset(1052, 492)

The import is `dart:ui` only (Rect, Offset, Color are all in dart:ui). Do NOT import
flutter/material.dart — Color is in dart:ui. The file must be pure const data with no
Flutter widget dependencies.

IMPORTANT: The territory name strings in kTerritoryData must match the names used as
keys in GameState.territories exactly. Verify against `mobile/lib/engine/models/map_schema.dart`
— the MapData territory names come from `assets/classic.json` which is the canonical source.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter analyze lib/widgets/map/territory_data.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    `territory_data.dart` compiles with 0 analyzer errors. `kTerritoryData.length == 42`
    (can be verified by adding a temporary `assert` or checking the count).
    `flutter analyze` reports no issues on this file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for map widget and interaction tests</name>
  <files>
    mobile/test/widgets/map/map_widget_test.dart,
    mobile/test/widgets/map/map_interaction_test.dart
  </files>
  <action>
Create two test stub files. These must COMPILE cleanly but use `skip:` markers on all
unimplemented tests so `flutter test` does not fail with unimplemented errors.

**`mobile/test/widgets/map/map_widget_test.dart`** — covers MAPW-01 and MAPW-03:
```dart
// ignore_for_file: unused_import
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:risk_mobile/widgets/map/territory_data.dart';

void main() {
  group('MapWidget rendering (MAPW-01, MAPW-03)', () {
    testWidgets(
      'MAPW-01: MapWidget renders inside InteractiveViewer without overflow',
      skip: 'Wave 0 stub — implement in plan 10-02',
      (tester) async {},
    );

    testWidgets(
      'MAPW-03: territory filled with owner color (player 0 = red)',
      skip: 'Wave 0 stub — implement in plan 10-02',
      (tester) async {},
    );

    testWidgets(
      'MAPW-03: army count label visible on territory',
      skip: 'Wave 0 stub — implement in plan 10-02',
      (tester) async {},
    );

    test('kTerritoryData contains exactly 42 territories', () {
      expect(kTerritoryData.length, 42);
    });

    test('kPlayerColors contains exactly 6 colors', () {
      expect(kPlayerColors.length, 6);
    });
  });
}
```

**`mobile/test/widgets/map/map_interaction_test.dart`** — covers MAPW-02, MAPW-04, MAPW-05:
```dart
// ignore_for_file: unused_import
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:risk_mobile/widgets/map/territory_data.dart';

void main() {
  group('Territory hit testing (MAPW-02, MAPW-05)', () {
    test(
      'MAPW-02: hit test at 1x zoom selects correct territory by rect containment',
      skip: 'Wave 0 stub — implement in plan 10-03',
      () {},
    );

    test(
      'MAPW-02: hit test at 2x zoom maps viewport coords to scene coords correctly',
      skip: 'Wave 0 stub — implement in plan 10-03',
      () {},
    );

    test(
      'MAPW-05: tap 4dp outside territory rect still selects it (6dp expansion)',
      skip: 'Wave 0 stub — implement in plan 10-03',
      () {},
    );

    test(
      'MAPW-05: tap in overlap of two expanded rects triggers disambiguation (returns multiple hits)',
      skip: 'Wave 0 stub — implement in plan 10-03',
      () {},
    );
  });

  group('Territory selection highlighting (MAPW-04)', () {
    testWidgets(
      'MAPW-04: selected territory shows highlight; valid targets show target tint',
      skip: 'Wave 0 stub — implement in plan 10-02',
      (tester) async {},
    );
  });
}
```

These stubs use `skip:` (string reason) on all tests except the two data-count assertions,
which run immediately and validate Task 1. The compile-only tests use skip, not `markTestSkipped`,
so the runner reports them as skipped rather than failing.
  </action>
  <verify>
    <automated>cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test test/widgets/map/ --no-pub 2>&1 | tail -10</automated>
  </verify>
  <done>
    `flutter test test/widgets/map/` exits 0. Two tests pass (kTerritoryData.length == 42,
    kPlayerColors.length == 6). All other tests report as skipped. No compilation errors.
  </done>
</task>

</tasks>

<verification>
Full suite still green:
```
cd /home/amiller/Repos/risk/mobile && /home/amiller/flutter-sdk/flutter/bin/flutter test --no-pub
```
Expected: 159+ tests (157 existing + 2 new passing), all skipped tests reported as skipped (not failed).
</verification>

<success_criteria>
- `mobile/lib/widgets/map/territory_data.dart` exists with `kTerritoryData` (42 entries),
  `TerritoryGeometry`, and `kPlayerColors` (6 colors)
- `flutter analyze lib/widgets/map/territory_data.dart` reports 0 errors
- `flutter test test/widgets/map/` exits 0: 2 passing, rest skipped
- Full suite: `flutter test` exits 0 with 159+ tests
</success_criteria>

<output>
After completion, create `.planning/phases/10-map-widget/10-01-SUMMARY.md`
</output>
